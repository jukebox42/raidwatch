import { Drawer, DrawerBody, DrawerCloseButton, DrawerContent, DrawerHeader, DrawerOverlay, Menu, MenuList } from "@chakra-ui/react"

type Props = {
  isOpen: boolean,
  onClose: () => void,
}

const RWMenu = ({ isOpen, onClose }: Props) => {
  return (
    <MenuList>
      
    </MenuList>
  )
}

export default RWMenu;
